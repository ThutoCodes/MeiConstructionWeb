// src/context/AuthContext.js (Temporary Mock)
export function useAuth() {
  // Simulate a user being logged in or not
  const user = { name: "Admin" }; // or null to simulate logged out
  return { user };
}